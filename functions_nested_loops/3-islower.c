#include "main.h"

/**
 * program to check for small letters
 * character checked @c
 * return 1 if the character checked is c,else return o
 * Author: Akong< Rodney 
 * Date: 11/20/2022
 * Time: 8:30;
 */
int _islower(int c);
{
	int v;

	(c >= 'a' && c <= 'z');

	v = c >= 'a' c <= 'z';
	return v;
}
